% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pct_scales.R
\name{x_pct}
\alias{x_pct}
\alias{y_pct}
\title{Percent-Formatted Axis Scales}
\usage{
x_pct(accuracy = 1, ...)

y_pct(accuracy = 1, ...)
}
\arguments{
\item{accuracy}{Number the percentage labels are rounded to, passed to
[scales::percent_format()]. The default `1` gives whole percentages.}

\item{...}{Additional arguments passed on to
[ggplot2::scale_x_continuous()] or [ggplot2::scale_y_continuous()]
(e.g. `limits`, `breaks`, `expand`).}
}
\value{
A ggplot2 scale that can be added to a plot with `+`.
}
\description{
Shortcuts for continuous axis scales with percentage labels, for plotting
the 0–1 proportions returned by [ct()] and [mean_ci()].
}
\examples{
library(dplyr)
library(ggplot2)

mtcars \%>\%
  ct(cyl) \%>\%
  ggplot(aes(x = factor(cyl), y = pct)) +
  geom_col() +
  y_pct()

}
